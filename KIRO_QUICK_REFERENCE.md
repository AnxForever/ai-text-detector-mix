# Kiro CLI 快速参考

## 🎯 最常用命令

| 命令 | 作用 | 使用场景 |
|------|------|----------|
| `/code init` | 初始化代码分析 | 开始项目开发时 |
| `/clear` | 清空对话 | 开始新话题 |
| `/compact` | 压缩历史 | 对话太长时 |
| `/plan` | 规划模式 | 分解复杂任务 |
| `/chat save` | 保存对话 | 重要讨论记录 |
| `/editor` | 打开编辑器 | 写长提示词 |
| `/context` | 管理上下文 | 添加参考文件 |
| `/model` | 切换模型 | 需要不同能力 |

## ⌨️ 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+J` | 多行输入 |
| `Ctrl+S` | 模糊搜索 |
| `Ctrl+T` | 独立对话 |
| `Shift+Tab` | 切换 agent |

## 🔧 代码智能 (/code)

```bash
/code init              # 初始化项目
/code goto <symbol>     # 跳转到定义
/code refs <symbol>     # 查找引用
/code symbols           # 列出所有符号
```

支持语言：Python, TypeScript, Rust, Go, Java, C/C++

## 💬 对话管理

```bash
/chat save <名称>       # 保存当前对话
/chat load              # 加载对话列表
/chat list              # 查看所有对话
/chat delete <名称>     # 删除对话
```

## 📂 上下文管理

```bash
/context add <文件>     # 添加文件到上下文
/context remove <文件>  # 移除文件
/context list           # 查看当前上下文
/context clear          # 清空上下文
```

## 🤖 Agent 切换

```bash
/plan                   # 规划 agent（任务分解）
/agent list             # 查看可用 agent
Shift+Tab               # 返回默认 agent
```

## 🛠️ MCP 服务器

MCP 可以扩展功能，配置文件位置：
- `~/.kiro/mcp-servers.json`
- 项目目录 `.kiro/mcp-config.json`

常用 MCP 服务器：
- `@modelcontextprotocol/server-filesystem` - 文件系统
- `@modelcontextprotocol/server-git` - Git 操作
- `@modelcontextprotocol/server-sqlite` - 数据库
- `@modelcontextprotocol/server-github` - GitHub API

查看状态：`/mcp`

## 📊 其他实用命令

```bash
/tools                  # 查看可用工具
/usage                  # 查看使用量
/experiment             # 实验性功能
/prompts                # 提示词模板
/todos                  # 待办事项
/changelog              # 更新日志
```

## 💡 使用技巧

1. **长对话优化**
   - 定期使用 `/compact` 压缩历史
   - 新话题用 `/clear` 清空

2. **代码开发**
   - 项目开始时运行 `/code init`
   - 直接问："找到 XXX 函数"

3. **任务规划**
   - 复杂任务先用 `/plan` 分解
   - 然后 `Shift+Tab` 回来执行

4. **保存重要内容**
   - 用 `/chat save` 保存对话
   - 用 `/context add` 添加参考文件

5. **多行输入**
   - 按 `Ctrl+J` 换行
   - 或使用 `/editor` 打开编辑器

---

**文档**: https://kiro.dev/docs/cli/
**MCP 配置**: https://kiro.dev/docs/cli/mcp
